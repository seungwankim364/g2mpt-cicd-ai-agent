# Copy values into your shell environment before running scripts/aws/stop-after-work.sh.
# Do not commit real credentials.

AWS_REGION=ap-northeast-2
TAG_KEY=Project
TAG_VALUE=cd-quality-gate
ENVIRONMENT=dev

# Default is dry-run. Add --execute to the script command when ready.
# scripts/aws/stop-after-work.sh --execute

