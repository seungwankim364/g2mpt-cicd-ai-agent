name: Execute Rollback

on:
  workflow_dispatch:
    inputs:
      deployment_id:
        required: true
        description: Deployment id from AI analysis
      service:
        required: true
        default: backend-api
      environment:
        required: true
        default: prod
      action_type:
        required: true
        default: rollback
      approved_by:
        required: true
      reason:
        required: true
      current_image_tag:
        required: false
        default: unknown
      target_image_tag:
        required: true
        description: Image tag to roll back to
      target_repo:
        required: false
        default: hj-3/gympt-gitops
        description: Repository that owns GitOps rollback workflow and GITOPS_PAT
      target_workflow:
        required: false
        default: rollback.yml
        description: Workflow file in target_repo that performs GitOps rollback
      app_repo:
        required: false
        default: hj-3/gympt-app
      app_workflow:
        required: false
        default: backend-api-ci.yml
      app_ref:
        required: false
        default: main

jobs:
  request-rollback:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    env:
      TARGET_REPO: ${{ inputs.target_repo }}
      TARGET_WORKFLOW: ${{ inputs.target_workflow }}
      DISPATCH_TOKEN: ${{ secrets.GH_WORKFLOW_DISPATCH_TOKEN }}
      APP_REPO: ${{ inputs.app_repo }}
      APP_WORKFLOW: ${{ inputs.app_workflow }}
      APP_REF: ${{ inputs.app_ref }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate rollback approval
        run: |
          test "${{ inputs.action_type }}" = "rollback"
          test -n "${{ inputs.target_image_tag }}"
          if [ -z "$DISPATCH_TOKEN" ]; then
            echo "GH_WORKFLOW_DISPATCH_TOKEN is required to call the existing GitOps rollback workflow." >&2
            exit 2
          fi

      - name: Write rollback request record
        run: |
          python3 - <<'PY'
          import json
          from datetime import datetime, timezone
          record = {
              "deploymentId": "${{ inputs.deployment_id }}",
              "service": "${{ inputs.service }}",
              "environment": "${{ inputs.environment }}",
              "actionType": "rollback",
              "approvedBy": "${{ inputs.approved_by }}",
              "reason": "${{ inputs.reason }}",
              "currentImageTag": "${{ inputs.current_image_tag }}",
              "targetImageTag": "${{ inputs.target_image_tag }}",
              "targetRepo": "${{ inputs.target_repo }}",
              "targetWorkflow": "${{ inputs.target_workflow }}",
              "startedAt": datetime.now(timezone.utc).isoformat(),
              "status": "dispatching_existing_gitops_rollback_workflow"
          }
          open("rollback-request.json", "w", encoding="utf-8").write(json.dumps(record, indent=2) + "\n")
          PY

      - name: Dispatch existing GitOps rollback workflow
        run: |
          scripts/github/dispatch-workflow.py \
            --repo "$TARGET_REPO" \
            --workflow "$TARGET_WORKFLOW" \
            --ref main \
            --token "$DISPATCH_TOKEN" \
            --input "deployment_id=${{ inputs.deployment_id }}" \
            --input "service=${{ inputs.service }}" \
            --input "environment=${{ inputs.environment }}" \
            --input "current_image_tag=${{ inputs.current_image_tag }}" \
            --input "target_image_tag=${{ inputs.target_image_tag }}" \
            --input "approved_by=${{ inputs.approved_by }}" \
            --input "reason=${{ inputs.reason }}"

      - name: Dispatch gympt-app deployment workflow
        run: |
          scripts/github/dispatch-workflow.py \
            --repo "$APP_REPO" \
            --workflow "$APP_WORKFLOW" \
            --ref "$APP_REF" \
            --token "$DISPATCH_TOKEN" \
            --input "service=${{ inputs.service }}" \
            --input "environment=${{ inputs.environment }}" \
            --input "image_tag=${{ inputs.target_image_tag }}" \
            --input "source_action=rollback" \
            --input "deployment_id=${{ inputs.deployment_id }}"

      - name: Upload rollback request record
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rollback-request
          path: rollback-request.json
