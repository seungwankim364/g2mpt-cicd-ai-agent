#!/usr/bin/env bash
set -euo pipefail
echo "Lambda duration high: check p95 duration, cold starts, memory size, downstream latency, VPC ENI delays, and payload size. Increase memory/timeout only with evidence."
